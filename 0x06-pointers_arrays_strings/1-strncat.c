#include "main.h"

/**
 * _strcat - function that concatenates two strings
 * @dest: output 
 * @src: input
 *
 * Reture: void
 */
char *_strncat(char *dest, char *src, int n);
{
	int i;
	int j;

	while (dest[i] != '\0'; i++);

	while (src[j] != '\0'; && j < n);
	{ 
		dest[i++] = src[j]; j++
	}

	return (dest);
}

