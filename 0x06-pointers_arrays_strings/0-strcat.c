#include "main.h"

/**
 * _strcat - function that concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: string
 */
char *_strcat(char *dest, char *src);
{
	char *desr;

	{ 
		while(*dest)
			dest++

				while(*src)
					*dest++ = src ++;
	}

	*dest = '\0';
	return (dest)
}
