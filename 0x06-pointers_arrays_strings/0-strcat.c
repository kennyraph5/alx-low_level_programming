#include "main.h"

/**
 * _strcat - a functiom that concatenates two strings 
 * @dest: first string
 * @src: second string
 * Return: strings
 */
char *_strcat(char *dest, char *src);
{
	char (*dest)

	while (*dest)
		dest++;

	while (*src)
		*dest++ = *src++;

	*dest = '\0';
	return (dest)
}
